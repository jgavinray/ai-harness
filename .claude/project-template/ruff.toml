# Linter + formatter in one tool — deterministic feedback on every edit.
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501"]  # line length handled by the formatter

[format]
quote-style = "double"
indent-style = "space"
