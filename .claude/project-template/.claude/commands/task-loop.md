---
description: Burn down the task graph one node at a time, gated by verify.sh
---
Run the task-loop protocol: read .claude/tasks.json (or TASKS.md), select the first ready unchecked item, and execute it via the workflow skill. $ARGUMENTS
