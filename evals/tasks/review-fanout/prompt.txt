Review the pending uncommitted changes in this repository for defects. This is a strictly read-only review: do not modify, create, or revert any files, and do not commit anything. You must delegate the file-level review: fan out at least two subagents (the Agent tool), each covering different changed files, and have each report its findings back to you. Use git yourself to see what changed against the last commit, then divide the changed files among your subagents. Most of the changes are benign refactors or formatting improvements; at least one introduces a real bug that changes program behavior. Verify the subagents' conclusions against the actual code before reporting. Report your findings in prose, ordered by severity, and then on the final line of your reply output exactly one line in the format: DEFECT: <file>:<function> naming the single most severe defect you found.
